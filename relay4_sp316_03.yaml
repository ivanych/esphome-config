substitutions:
  <<: !include secrets.yaml

  device_number: "03"
  device_unit: "relay4"
  device_board: "sp316"
  device_version: "5.0.1"
  device_author: "ivanych"
  device_timezone: "Europe/Moscow"

packages:
  devices:
    url: https://github.com/ivanych/esphome-devices
    files:
      - ${device_unit}_${device_board}.yaml
    ref: ${device_version}
    #!include ../esphome-devices/${device_unit}_${device_board}.yaml
